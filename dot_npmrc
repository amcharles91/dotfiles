# NPM Configuration
# This tells npm to install global packages to ~/.npm-global instead of system directories
prefix=${HOME}/.npm-global

# Optional: Set default npm init values
# init-author-name=Your Name
# init-author-email=your.email@example.com
# init-license=MIT